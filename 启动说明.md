# uniobfuscator 启动与使用说明

多语言代码混淆工具，支持 **Python / JavaScript / Java（源码）** 与 **JAR 字节码** 混淆。

---

## 一、环境要求

- Windows 11（本项目开发环境）
- Python 3.10+（TOML 配置需 Python 3.11+，或自行安装 tomli）

## 二、安装

```powershell
# 1. 创建虚拟环境（项目根目录下执行）
python -m venv .venv

# 2. 激活虚拟环境
.venv\Scripts\activate

# 3. 安装依赖（含 GUI 依赖 flet）
pip install -e .[gui]

# 若不需要图形界面，只装核心依赖即可
pip install -e .
```

> 工作区规则：本项目使用 Python 时必须使用虚拟环境 `.venv`。
> 如果 `pip install` 不可用，可用 `python -m pip install ...`。

---

## 三、命令行启动（CLI）

### 方式 1：安装后直接使用命令（推荐）

```powershell
uniobfuscator [参数]
```

### 方式 2：用 python 模块方式启动（无需安装命令）

```powershell
python -m uniobfuscator.cli [参数]
```

### 查看帮助

```powershell
uniobfuscator --help
uniobfuscator --list-languages        # 列出支持的语言
```

---

## 四、图形界面启动（GUI）

```powershell
# 方式 1：安装后使用命令
uniobfuscator-gui

# 方式 2：模块方式
python -m uniobfuscator.gui
```

> GUI 需要 flet 依赖（`pip install -e .[gui]`）。首次启动会自动生成
> `config/uniobfuscator.toml` 默认配置文件。

---

## 五、混淆模式与开关切换

程序按**输入形态自动切换**混淆开关，同一份参数对多语言通用：

| 输入形态 | 生效的开关 | 不生效（会提示并忽略） |
|---|---|---|
| `.py` / `.js` / `.java` 源文件 | `--rename` `--strings` `--dead-code` `--arithmetic` | `--java-*` 系列 |
| `.jar` 字节码 | `--strings` + `--java-*` 系列 | `--rename` `--dead-code` `--arithmetic` |

---

## 六、命令行参数详解

### 通用参数

| 参数 | 说明 |
|---|---|
| `path`（位置参数） | 待混淆的源文件或目录（或 .jar 文件） |
| `-l, --language` | 语言：`python` / `javascript` / `java`（默认按扩展名自动识别） |
| `-o, --output` | 输出路径：单文件=文件路径；目录/JAR=输出目录或输出 jar |
| `--stdout` | 单文件结果输出到屏幕，不写文件 |
| `--seed N` | 随机种子，保证可复现（默认 0） |
| `--exclude CLASS` | JAR 模式跳过不混淆的类/包（可重复） |
| `-c, --config FILE` | 配置文件路径（.json / .toml），命令行参数优先于配置文件 |
| `--list-languages` | 列出支持的语言 |

### 文本源码混淆开关（.py / .js / .java）

| 参数 | 默认 | 说明 |
|---|---|---|
| `--rename` / `--no-rename` | 开 | 标识符重命名（函数内局部变量/参数） |
| `--strings` / `--no-strings` | 开 | 字符串加密（JAR 模式同样适用） |
| `--dead-code` / `--no-dead-code` | 开 | 死代码注入 |
| `--arithmetic` / `--no-arithmetic` | 开 | 算术混淆 |

### JAR 字节码混淆开关（.jar）

| 参数 | 默认 | 说明 |
|---|---|---|
| `--java-arithmetic` / `--no-java-arithmetic` | 开 | 整型常量算术混淆 |
| `--java-dead-code` / `--no-java-dead-code` | 开 | 死代码注入（不透明谓词，仅无分支方法） |
| `--java-scramble` / `--no-java-scramble` | 开 | 控制流打散（栈平衡垃圾块 + goto 绕行） |
| `--java-strip-metadata` / `--no-java-strip-metadata` | 开 | 剥离元数据（泛型签名 / throws / 不可见注解） |
| `--java-rename` / `--no-java-rename` | 关 | 类名重命名（保留包路径） |
| `--java-member-rename` / `--no-java-member-rename` | 关 | 私有方法/字段重命名（Serializable 字段自动保护） |
| `--java-repackage` / `--no-java-repackage` | 关 | 包名混淆：平铺到单一短包 + 全局短类名（破坏性最强） |

> **JAR 模式安全机制**：开启改名类 pass（`--java-rename` / `--java-repackage`）
> 与成员重命名时，自动做反射保护分析 —— 被 `Class.forName` / `getMethod`
> 等按名字符串引用的类与成员自动跳过，无需手工处理。

---

## 七、常用示例

### 1. 混淆单个 Python 文件

```powershell
uniobfuscator app.py -o app_obf.py
```

### 2. 混淆 JavaScript 文件，输出到标准输出、关闭重命名

```powershell
uniobfuscator app.js --no-rename --stdout
```

### 3. 混淆整个目录（镜像结构、保持文件名）

```powershell
uniobfuscator src/ -o out/
```

### 4. 目录模式下只混淆 Java 文件

```powershell
uniobfuscator src/ -o out/ -l java
```

### 5. 使用配置文件 + 命令行覆盖（命令行优先）

```powershell
uniobfuscator -c conf.json src/ --seed 9
```

### 6. 基本 JAR 混淆（默认开关）

```powershell
uniobfuscator app.jar -o app_obf.jar
```

### 7. JAR 开启类名重命名

```powershell
uniobfuscator app.jar --java-rename -o app_obf.jar
```

### 8. JAR 全开高级混淆（包名混淆 + 私有成员重命名 + 元数据剥离）

```powershell
uniobfuscator app.jar --java-repackage --java-member-rename -o app_obf.jar
```

### 9. JAR 排除指定类/包（反射/资源类原样保留）

```powershell
uniobfuscator app.jar --exclude com.foo.Secret --exclude com.foo.secret.* -o app_obf.jar
```

---

## 八、配置文件

- **默认配置**：启动时自动生成 `config/uniobfuscator.toml`（若不存在），编辑后自动生效。
- **自定义配置**：用 `-c <文件>` 指定（.json / .toml），命令行参数优先。

优先级：**命令行参数 > 按语言配置 > 全局配置 > 语言能力默认值**。

### 默认配置模板（自动生成内容）

```toml
# ---------------- 全局配置 ----------------
# path = "src"                # 待混淆的源文件或目录
# language = "python"         # python / javascript / java
# output = "out"              # 输出路径
# seed = 0                    # 随机种子，保证可复现

# 文本源码混淆开关（默认全部开启）
rename = true
strings = true
dead_code = true
arithmetic = true

# JAR 排除列表：精确类名 'com.foo.Secret' 或包前缀 'com.foo.secret.*'
# exclude = ["com.foo.Secret", "com.foo.secret.*"]

# JAR 字节码开关（默认除改名类 pass 外全部开启）
# java_arithmetic = true
# java_dead_code = true
# java_scramble = true
# java_strip_metadata = true
# java_rename = false
# java_repackage = false
# java_member_rename = false

# ---------------- 按语言配置（覆盖全局） ----------------
# [languages.python]
# rename = false
```

---

## 九、开发与测试

```powershell
# 运行全部测试
python -m pytest -q

# 运行单个测试文件
python -m pytest tests/test_jar.py -q
```
